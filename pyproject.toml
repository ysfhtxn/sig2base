[project]
name = "sig2base"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "evaluate>=0.4.0",
    "jiwer>=3.0.0",
    "peft>=0.18.1",
    "torch>=2.10.0",
    "transformers>=5.3.0",
]
